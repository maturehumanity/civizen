/** Public Jobs entry. `/jobs` redirects to Market > Jobs so the website has a shareable labor URL. */
export const PUBLIC_JOBS_PATH = '/jobs';
export const PUBLIC_JOBS_MARKET_HREF = '/market?section=jobs';
