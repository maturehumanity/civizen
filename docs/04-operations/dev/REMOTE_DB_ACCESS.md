# Remote database access

Detailed remote database access, migration runbooks, and host-specific overrides live in the **access-controlled operations store**.

This public repository does not authorize or describe production database access. Local development uses the documented local environment and local Supabase/dev setup.

When restricted ops configuration is available to the agent, apply remote migrations privately without publishing details.
