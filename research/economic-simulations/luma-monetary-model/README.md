# Luma monetary model (research)

**Non-operational.** This folder holds exploratory issuance / policy simulation code formerly shipped under `src/lib/monetary.ts`.

The Civizen app must not import these modules. Production surfaces use `src/lib/prototype-credits.ts` only (display helpers and the controlling prototype notice).
