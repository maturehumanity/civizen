#!/usr/bin/env bash
# One-time agent SSH key install for the managed server.
# Detailed hostnames, key filenames, and steps are maintained in the restricted operations store.
echo "This public repository does not publish production SSH install steps." >&2
echo "Use the restricted operations runbook for agent key installation." >&2
exit 1
